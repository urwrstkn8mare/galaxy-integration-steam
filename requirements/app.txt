galaxy.plugin.api==0.69

# steam_network backend
vdf==3.4
pyobjc-framework-CoreServices==7.3; sys_platform == 'darwin' #if feeling adventurouse bump to 9.1.1. 9.2 has a major bug so avoid it. 
certifi; sys_platform == 'darwin'
websockets==11.0
dataclasses-json==0.5.7
rsa==4.9

#better protobufs so i don't go insane.
betterproto==1.2.5

# local for macos (for now)
file-read-backwards==3.0.0; sys_platform == 'darwin'

