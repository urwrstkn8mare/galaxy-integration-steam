#use specific pip version for dependencies to work nicely
pip==22.0.4

#protoc install.
requests

#install requirements. 
invoke==1.2.0
wheel

galaxy.plugin.api==0.69

certifi==2023.5.7; sys_platform == 'darwin' #required to avoid certification issues. Afaik version unimportant, but you can never be sure because Python!